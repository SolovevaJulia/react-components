# 📌 React Component Portfolio

Этот репозиторий содержит примеры моих разработок на **React**. Здесь представлен код одного из модальных окон, которое я писала в продакшн-проекте. Данный компонент демонстрирует мой подход к работе с **React, хуками, управлением состоянием и UI-библиотеками**.

## 📂 Структура проекта
```
📦 react-portfolio
│── 📁 components              # UI-компоненты
│   ├── OrderForm.jsx          # Родительский компонент (модальное окно)
│   ├── PlanUploading.jsx      # Дочерний компонент (загрузка файлов)
│   ├── OrderSummary.jsx       # Итоговый шаг заказа
│   ├── PaymentStep.jsx        # Этап оплаты
│   ├── Payment.jsx            # Форма оплаты
│   ├── PropertySelection.jsx  # Выбор типа недвижимости
│   ├── StyleSelection.jsx     # Выбор стиля интерьера
│── 📁 store                   # Управление состоянием (MobX)
│   ├── StepOrderStore.js      # Хранение данных заказа
│── README.md                  # Этот файл

## 🛠 Используемые технологии
- **React (Hooks, Functional Components)**
- **MobX (управление состоянием)**
- **Загрузка файлов и работа с input[type='file']**

## 🔹 Описание компонентов
### 📁 `components/OrderForm.jsx`
Родительский компонент, который:
- Управляет шагами процесса оформления заказа
- Визуализирует прогресс шагов
- Использует MobX (`StepOrderStore`) для управления состоянием
- Подключает дочерние компоненты в зависимости от текущего шага

### 📁 `components/PlanUploading.jsx`
Дочерний компонент, который:
- Позволяет загружать файлы (изображения, PDF)
- Управляет списком загруженных файлов
- Выполняет валидацию и обработку ошибок
- Содержит интерактивные UI-элементы (инфо-иконки, модальные окна)

### 📁 `components/OrderSummary.jsx`
Этап подтверждения заказа:
- Отображает выбранные параметры
- Позволяет ввести промокод
- Рассчитывает стоимость

### 📁 `components/PaymentStep.jsx` и `Payment.jsx`
Шаг оплаты:
- Отображает iframe с платёжной формой
- Показывает сообщение, если ссылка на оплату отсутствует

### 📁 `components/PropertySelection.jsx`
Шаг выбора типа недвижимости:
- Позволяет выбрать тип жилья (дом, квартира, другое)
- Управляет количеством этажей / квартир

### 📁 `components/StyleSelection.jsx`
Шаг выбора стиля интерьера:
- Выбор интерьера, типа окон и вида из окна
- Использует кастомный селект

### 📁 `store/StepOrderStore.js`
MobX-стор, который хранит и управляет данными оформления заказа:
- `currentStep` – текущий шаг
- `stepsFlags` – флаги пройденных шагов
- `plans` – загруженные файлы
- Методы переходов между шагами (`openNextStep`, `openPrevStep`)

## ⚠️ Важное замечание
Этот компонент **не является полностью рабочим приложением**, а лишь пример моего кода в рамках реального проекта. Полный проект я не могу выложить в открытый доступ.

